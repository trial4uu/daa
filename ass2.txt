merged=pd.concat([df1,df2], axis=1)
print(merged.head()) 

sorted=df.sort_values(by=['like'],ascending=False)
sorted.head()


import numpy as ny

arr=ny.array(df[['Post Month','Post Weekday']].head(6))
reshaped=arr.reshape(3,4)
print(reshaped)
